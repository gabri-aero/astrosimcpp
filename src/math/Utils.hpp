#include <stdexcept>
#include <math.h>

#ifndef _UTILS_HPP_
#define _UTILS_HPP_

unsigned long long factorial(int n);
int C(int n, int k);
double rad2deg(double rad);
double deg2rad(double deg);

#endif //_UTILS_HPP_